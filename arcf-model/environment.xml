<?xml version='1.0' encoding='utf-8'?>

<scene version="0.5.0">

    <!-- fallback lightsource to be able to see what's happening -->
    <emitter type="sunsky" />

    <!-- floor -->

    <shape type="rectangle" id="floor">
        <bsdf type="diffuse">
            <texture type="checkerboard" name="reflectance">
                <float name="uvscale" value="5"/>
            </texture>
        </bsdf>
        <transform name="toWorld">
            <scale value="5"/>
            <rotate x="1" angle="-90"/>
            <translate y="-1"/>
        </transform>
    </shape>

    <!--
    <shape type="instance">
        <ref id="axes"/>
        <transform name="toWorld">
            <scale value="1"/>
        </transform>
    </shape>
    -->

</scene>
